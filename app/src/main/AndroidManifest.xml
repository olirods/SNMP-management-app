<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clientesnmp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_tabla_stp"></activity>
        <activity android:name=".Activity_tabla_if" />
        <activity android:name=".Activity_tabla_tcp" />
        <activity android:name=".Activity_tabla_udp" />
        <activity android:name=".Activity_tabla_route" />
        <activity
            android:name=".TrapActivity"
            android:label="Recepción de traps"
            android:launchMode="singleTop" />
        <activity
            android:name=".Activity_tabla_ip"
            android:label="Tabla de direcciones IP" />
        <activity android:name=".PruebaSet" />
        <activity
            android:name=".OtraActivity"
            android:label="@string/title_activity_otra"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="Gestor de dispositivos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>